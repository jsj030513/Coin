# 관리자 운영

- 신규 사용자는 관리자 콘솔에서 24시간 초대코드를 발급받아 가입하고, 관리자가 승인해야 로그인할 수 있다.
- 관리자 로그인은 비밀번호 다음 TOTP 인증을 요구한다. 관리자 계정의 텔레그램도 연결하면 로그인 실패 알림을 받는다.
- 전체 비상정지는 모든 일반 사용자의 자동매매를 끈다. 다시 켜기는 각 사용자가 직접 수행한다.
- H2 온라인 백업은 매일 03:10 KST 실행되고 최근 14개를 보관한다.
- 복구는 서버에서 `scripts/restore-h2-backup.sh <zip>`을 실행한다. 복구 전 현재 DB가 별도 보관된다.
- `data/account-credentials.key`는 API 키 복호화에 필수다. DB 백업과 별도로 안전하게 보관하며 공개 저장소에 넣지 않는다.
- 암호화 키 교체는 모든 거래소 키를 재등록하는 유지보수 창에서 수행한다. 키 파일만 교체하면 기존 키를 복호화할 수 없다.
